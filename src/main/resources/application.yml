spring:
  datasource:
    url: jdbc:sqlserver://dbprojectserver.database.windows.net:1433;database=db2;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30
    username: byuntil@dbprojectserver
    password: Asdf2002
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.SQLServerDialect
        format_sql: true

# If you have multiple profiles, you can set active profile
# spring:
#   profiles:
#     active: prod